export async function signIn(username, password) {
    return Promise.resolve (true);
}

export async function signOut() {
    return Promise.resolve (true);
}

export async function doRestoreSession() {
    //todo: update to supabase structure
    return Promise.resolve (true);
}